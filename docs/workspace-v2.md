# Workspace v2

This development line turns FCC Assistant from a single-unit demo shell into a locally configurable operations cockpit.

Current scope:

- versioned local TRAVIS bridge
- local multi-unit semantic site configuration
- existing natural-language dashboard planner
- dynamic trend/KPI/average/summary widgets
- unit scope UI foundation

Next scope:

- wire the desktop scope selector into App state
- per-unit/per-role workspaces
- local speech-to-text
- embedded local AI completion
- PI Web API production configuration

All process integration remains read-only.
